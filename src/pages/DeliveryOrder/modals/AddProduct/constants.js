/**
 * Initial state for add product modal
 * @type {{product: string, code: string, quantity: number, price: number}}
 */
export const INITIAL_STATE = {
  code: '',
  product: '',
  quantity: 0,
  price: 0,
};
